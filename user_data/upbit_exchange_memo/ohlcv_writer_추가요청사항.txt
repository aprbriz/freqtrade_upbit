*. config.json 에서 enable_ws = false 로 바꿨음. 네가 워낙 잘해줘서 굳이 true 를 할 필요가 없어졌음
   upbit 는 요청수인가 제한도 있잖아.
**. 다 쓰고 나니 요청사항/질문이 너무 많네(나도 이것 쓰는데 꽤 오래 걸리긴 했음 ^^;) "고수님". 밥이라도 한끼 사주고 싶은데.. ^^
***. 내 이번 요청사항이 적용된 부분은 별도 주석처리 해주면 고맙겠다
****. 내가 쓰는 오라클 클라우드는 core 3, hdd 200GB 로 이것저것 빡쌔게 돌릴 사양은 충분함.

0. 전에 말을 잘못 했는데 1초가 아니고 0.5초임. 즉, 0.5초 봉으로 바꿔야겠어.
   앞으로 3틱봉, 33초봉, 57초봉, 1분봉, ... 도 만들건데
   일단 이번 소스에서 0.5초와 1초봉을 적용하는 것도 괜찮을 것 같음. "고수님" 의견은?
   (중기적으로는 0.5초봉, 1초봉, 3틱봉, 33초봉, 57초봉, 1분봉 데이터 수집을 config file 에서 지정하고,
   장기적으로는 0.5초봉, 1초봉, 3틱봉, 33초봉, 57초봉, 1분봉 중 데이터 수집중인 것의 차트는 웹페이지에서 선택할 수 있게
   만들 생각이야)

1. symbol 별로 테이블을 나눠서 저장해주는 코드로 바꿔주라.
단.. primary key는 symbol, ts 로 설정해줄 수 밖에 없으니 table 에 symbol 컬럼은 그대로 둬야겠네.

그런데 이렇게 하면 PK 는 지정하되 symbol 컬럼을 없애는 방법은 없을까? 인덱스도 하나 뼐 수 있으니 (지금은 2개 걸었지?)
예를 들어 PK를 1천만 row(sqlite 가 이정도까지 지원하려나? ^^;)까지 커버할 수 있도록
random (숫자열이 아니고) 문자열로ㅎㅎ. 
이경우 index 는 ts만 걸면 되겠네.
(실은 내가 OCP DBA 자격증 보유자임 --v )


2. freqtrade 에서 변수를 symbol 이 아니라 pair 로 사용하고 있으면 모두 pair 로 변경해주라.
   우리 code 에서는는 symbol 과 pair 를 혼용하고 있는 것 같은데.. ^^;

3. "def cleanup_old_data(self, days: int = 365): 으로 해줘.
   - 이게 나중에는 config file 에서 설정할 수 있도록 할 것인데 
   - 지금은 그런 코드는 작성하지 말고 일단 하드코딩으로 해줘.
   - symbol 별로 테이블을 나눠서 저장하라고 한 이유 중 하나임 ^^

4. 그리고 table 에 저장하는 time 을 내 oracle 클라우드 시간으로 하는 것 같은데..
   websocket 으로 받은 시간으로 저장하면 안되겠지? 데이터는 틱으로 받는데 0.5초봉과 1초봉을 저장해야 하니깐.
   네 의견을 말해주라.

5. 아래 링크와 같이 upbit 도 1초 ohlcv 를 websocket 으로 subscribe 할 수 있어.
   https://docs.upbit.com/kr/reference/websocket-candle
   그런데.. 아래 코드를 보면 우리 코드는 trade 를 구독하고 있어. 
   내가 0.5초봉, 3틱봉 등도 만들기 원하기 때문에 결국 trade 를 구독해야 하는데.. 
   나중에 1분봉, 10분봉, 60분봉... 등을 받을 때는 upbit ohlcv(candle) 을 받는 것이 좋을까?
   (나중에 할 얘기지만 일단 말 나온 김에.. ^^)

    subscribe = [
            {"ticket": "freqtrade-ohlcv"},
            {
                "type": "trade",           # 체결 데이터
                "codes": self.pairs,       # 구독할 마켓 목록
                "isOnlyRealtime": True
            },

